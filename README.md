setelah clone, masuk ke terminal kodingannya

lalu di terminal, ketik perintah dibawah ini:

1. _python -m venv venv_
   
2. _source venv/bin/activate  # Untuk Linux/Mac_
  _ venv\Scripts\activate     # Untuk Windows_

3. _pip install -r requirements.txt_
4. _python main.py_
